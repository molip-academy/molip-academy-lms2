# CLAUDE.md

The instructions for this repo live in `AGENTS.md` so that every agent reads the same file. Follow it.

@AGENTS.md

Claude-specific notes, if any are ever needed, go below this line. Anything that applies to all agents belongs in `AGENTS.md` instead.
